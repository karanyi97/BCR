To run the app, run these commands in a linux terminal

1) In the BCR directory containing the pom.xml, run "mvn install" and "mvn package"
2) In the target directory, run "java -jar BCR-0.0.1-SNAPSHOT.jar <FILE-PATH>"